{
  "name": "despia-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "The despia.com landing page - a DSX web project built by the shipped toolchain.",
  "scripts": {
    "build": "dsx build",
    "dev": "dsx dev",
    "lint": "dsx lint --strict"
  }
}
